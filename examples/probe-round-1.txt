PLAN:
1. On workstation (the session host, assumed to own the cron job), locate the job and its log: `crontab -l`, `/etc/cron.d`, `systemctl list-timers`, then read the script to get the rsync source, the target (LAN IP or offsite hostname decides whether WoL is even possible), whether `--timeout` is rsync's I/O timeout (does not cover SSH connect) or a coreutils `timeout` wrapper, and the log path plus its logrotate rule. Also `ps -ef | grep -E 'rsync|ssh'` for a hung rsync from an earlier night still alive.
2. Before 04:00 on 2026-09-13, read the 2026-09-12 03:15 log (and the rotated `.1` if it exists): no file lines after the SSH start means connect hang, file lines then silence means mid-transfer. Cross-check on the NAS side with `ssh nas uptime` and its event log to learn whether the 03:00 sleep is a power schedule or idle hibernation (assumption: the rsync SSH key gives me a shell there).
3. Today, safely during the day (no transfer): harden the script so the next failure is bounded and diagnosable: `flock -n` against overlap, per-run timestamped log in a non-rotated file such as `backup-YYYY-MM-DD.log`, and `-e "ssh -o ConnectTimeout=30 -o ServerAliveInterval=15 -o ServerAliveCountMax=4"` so a connect hang fails fast. Commit if the script lives in a repo.
4. Choose the fix by evidence: connect hang with NAS on the same LAN and WoL supported (`ssh nas ethtool <if> | grep Wake-on`, MAC from `arp -n`) means prepend `wakeonlan <MAC>` plus a wait loop on `nc -z nas 22` up to about 5 minutes; idle hibernation or mid-transfer hang means move cron to 02:30 only if the instrumented log shows runs finish in under 30 minutes, otherwise keep WoL or a keepalive. Verify on the next night from the per-run log; never run rsync by hand in daytime, but `wakeonlan` and `ssh nas true` tests are fine.
5. Assumptions adopted: I do not touch the NAS's own power schedule (household-shared) without asking; if the target turns out to be offsite, WoL is dropped and the fix is schedule move plus fast-fail.

QUESTIONS:
NO-BLOCKING-QUESTIONS
